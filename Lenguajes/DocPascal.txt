The Language Pascal
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Pascal==

===Literals===
Integer literals //Integer// are nonempty sequences of digits.


Double-precision float literals //Double// have the structure
indicated by the regular expression ``digit+ '.' digit+ ('e' ('-')? digit+)?`` i.e.\
two sequences of digits separated by a decimal point, optionally
followed by an unsigned or negative exponent.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.


Character literals //Char// have the form
``'``//c//``'``, where //c// is any single character.




Id literals are recognized by the regular expression
`````letter (letter | digit)*`````


===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Pascal are the following:
  | ``and`` | ``array`` | ``begin`` | ``case``
  | ``const`` | ``div`` | ``do`` | ``downto``
  | ``else`` | ``end`` | ``for`` | ``function``
  | ``if`` | ``mod`` | ``not`` | ``of``
  | ``or`` | ``procedure`` | ``program`` | ``record``
  | ``repeat`` | ``then`` | ``to`` | ``type``
  | ``until`` | ``var`` | ``while`` | 

The symbols used in Pascal are the following:
  | ; | . | = | :
  | ( | ) | .. | ^
  | [ | ] | , | :=
  | > | < | >= | <=
  | <> | - | + | *
  | / |  |  | 

===Comments===
There are no single-line comments in the grammar.Multiple-line comments are  enclosed with (* and *).

==The syntactic structure of Pascal==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Program// | -> | ``program`` //Id// ``;`` //Block// 
  | //Block// | -> | //Parts// //Body// ``.`` 
  | //Parts// | -> | //Consts// //Types// //Vars// //[ProcsYFuncs]// 
  | //Consts// | -> | **eps**
  |  |  **|**  | ``const`` //[Const]// //Const// ``;`` 
  | //Const// | -> | //Id// ``=`` //Literal// 
  | //Literal// | -> | //Integer// 
  |  |  **|**  | //Double// 
  |  |  **|**  | //String// 
  |  |  **|**  | //Char// 
  | //Vars// | -> | **eps**
  |  |  **|**  | ``var`` //[Var]// //Var// ``;`` 
  | //Var// | -> | //[Id]// ``:`` //CustomType// 
  | //Types// | -> | **eps**
  |  |  **|**  | ``type`` //[Type]// //Type// ``;`` 
  | //Type// | -> | //Id// ``=`` //CustomType// 
  | //CustomType// | -> | ``(`` //[Id]// ``)`` 
  |  |  **|**  | //RangeType// ``..`` //RangeType// 
  |  |  **|**  | ``^`` //Id// 
  |  |  **|**  | ``array`` ``[`` //[ArrType]// ``]`` ``of`` //CustomType// 
  |  |  **|**  | ``record`` //[Fields]// ``end`` 
  |  |  **|**  | //Id// 
  | //RangeType// | -> | //Id// 
  |  |  **|**  | //Literal// 
  | //[ArrType]// | -> | **eps**
  |  |  **|**  | //ArrType// 
  |  |  **|**  | //ArrType// ``,`` //[ArrType]// 
  | //ArrType// | -> | //Id// 
  |  |  **|**  | //Literal// 
  |  |  **|**  | //RangeType// ``..`` //RangeType// 
  | //[Fields]// | -> | **eps**
  |  |  **|**  | //Fields// 
  |  |  **|**  | //Fields// ``;`` //[Fields]// 
  | //Fields// | -> | //[Id]// ``:`` //CustomType// 
  | //[ProcsYFuncs]// | -> | **eps**
  |  |  **|**  | //ProcsYFuncs// //[ProcsYFuncs]// 
  | //ProcsYFuncs// | -> | ``procedure`` //Id// ``(`` //[DecParm]// ``)`` ``;`` //BlockProcFun// ``;`` 
  |  |  **|**  | ``function`` //Id// ``(`` //[DecParm]// ``)`` ``:`` //Id// ``;`` //BlockProcFun// ``;`` 
  | //[DecParm]// | -> | **eps**
  |  |  **|**  | //DecParm// 
  |  |  **|**  | //DecParm// ``;`` //[DecParm]// 
  | //DecParm// | -> | ``var`` //[Id]// ``:`` //Id// 
  |  |  **|**  | //[Id]// ``:`` //Id// 
  | //BlockProcFun// | -> | //Parts// //Body// 
  | //Body// | -> | ``begin`` //[Instruction]// ``end`` 
  | //[Instruction]// | -> | **eps**
  |  |  **|**  | //Instruction// 
  |  |  **|**  | //Instruction// ``;`` //[Instruction]// 
  | //Instruction// | -> | //SimpleInstruction// 
  |  |  **|**  | //StructuredInstruction// 
  | //StructuredInstruction// | -> | //CompositeInstruction// 
  |  |  **|**  | //ConditionalInstruction// 
  |  |  **|**  | //RepetitiveInstruction// 
  | //SimpleInstruction// | -> | //[AccId]// ``:=`` //Exp// 
  |  |  **|**  | //CallProc// 
  | //ConditionalInstruction// | -> | ``if`` //Exp// ``then`` //Instruction// 
  |  |  **|**  | ``if`` //Exp// ``then`` //Instruction// ``else`` //Instruction// 
  |  |  **|**  | ``case`` //Exp// ``of`` //[Ramas]// ``end`` 
  | //RepetitiveInstruction// | -> | ``repeat`` //[Instruction]// ``until`` //Exp// 
  |  |  **|**  | ``for`` //Id// ``:=`` //Exp// ``to`` //Exp// ``do`` //Instruction// 
  |  |  **|**  | ``for`` //Id// ``:=`` //Exp// ``downto`` //Exp// ``do`` //Instruction// 
  |  |  **|**  | ``while`` //Exp// ``do`` //Body// 
  | //CompositeInstruction// | -> | //Body// 
  | //[Ramas]// | -> | //Ramas// 
  |  |  **|**  | //Ramas// ``;`` //[Ramas]// 
  | //Ramas// | -> | //[ConstCase]// ``:`` //BodyRamaCase// 
  | //[ConstCase]// | -> | **eps**
  |  |  **|**  | //ConstCase// 
  |  |  **|**  | //ConstCase// ``,`` //[ConstCase]// 
  | //ConstCase// | -> | //Literal// 
  |  |  **|**  | //Id// 
  | //BodyRamaCase// | -> | //Instruction// 
  |  |  **|**  | //Body// 
  | //CallFunProc// | -> | //Id// ``(`` //[Exp]// ``)`` 
  | //CallProc// | -> | //Id// ``(`` //[ExpC]// //ExpC// ``)`` 
  |  |  **|**  | //Id// 
  | //ExpC// | -> | //Exp// 
  | //[ExpC]// | -> | **eps**
  |  |  **|**  | //ExpC// ``,`` //[ExpC]// 
  | //[Exp]// | -> | **eps**
  |  |  **|**  | //Exp// 
  |  |  **|**  | //Exp// ``,`` //[Exp]// 
  | //Exp// | -> | ``not`` //Exp1// 
  |  |  **|**  | //Exp1// 
  | //Exp1// | -> | //Exp1// //GenCom// //Exp2// 
  |  |  **|**  | //Exp2// 
  | //GenCom// | -> | ``>`` 
  |  |  **|**  | ``<`` 
  |  |  **|**  | ``=`` 
  |  |  **|**  | ``>=`` 
  |  |  **|**  | ``<=`` 
  |  |  **|**  | ``<>`` 
  | //Exp2// | -> | ``-`` //Exp3// 
  |  |  **|**  | ``+`` //Exp3// 
  |  |  **|**  | //Exp2// //AddCom// //Exp3// 
  |  |  **|**  | //Exp3// 
  | //AddCom// | -> | ``+`` 
  |  |  **|**  | ``or`` 
  |  |  **|**  | ``-`` 
  | //Exp3// | -> | //Exp3// //MulCom// //Exp4// 
  |  |  **|**  | //Exp4// 
  | //MulCom// | -> | ``*`` 
  |  |  **|**  | ``/`` 
  |  |  **|**  | ``div`` 
  |  |  **|**  | ``mod`` 
  |  |  **|**  | ``and`` 
  | //Exp4// | -> | //Literal// 
  |  |  **|**  | //AccId// 
  |  |  **|**  | //AccId// ``.`` //[AccId]// 
  |  |  **|**  | //CallFunProc// 
  |  |  **|**  | ``(`` //Exp// ``)`` 
  | //[AccId]// | -> | //AccId// 
  |  |  **|**  | //AccId// ``.`` //[AccId]// 
  | //AccId// | -> | //Id// 
  |  |  **|**  | //Id// ``^`` 
  |  |  **|**  | //ArrayAccess// 
  | //ArrayAccess// | -> | //Id// ``[`` //[TypeAccess]// ``]`` 
  | //[TypeAccess]// | -> | //TypeAccess// 
  |  |  **|**  | //TypeAccess// ``,`` //[TypeAccess]// 
  | //TypeAccess// | -> | //Exp// 
  | //[Const]// | -> | **eps**
  |  |  **|**  | //Const// ``;`` //[Const]// 
  | //[Var]// | -> | **eps**
  |  |  **|**  | //Var// ``;`` //[Var]// 
  | //[Type]// | -> | **eps**
  |  |  **|**  | //Type// ``;`` //[Type]// 
  | //[Id]// | -> | **eps**
  |  |  **|**  | //Id// 
  |  |  **|**  | //Id// ``,`` //[Id]// 


